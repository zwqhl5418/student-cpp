# Task 6: HTML 模板渲染 — 实施报告

## 创建的文件

- `src/template.h` — 模板接口声明
- `src/template.cpp` — 完整 HTML 渲染实现（含内联 CSS 深色科幻主题）

## 编译命令

```
g++ -std=c++11 -Wall -Wextra -Isrc -c src/template.cpp -o build/template.o
```

## 输出/错误

无输出，编译成功（零错误，零警告）。
